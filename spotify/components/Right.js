import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Right = () => {
  return (
    <View style={{flex: 0.8}}>
      <Text> Hello Danny</Text>
    </View>
  );
};

export default Right;

const styles = StyleSheet.create({});
