import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Test = ({item, drag, isActive}) => {
  return (
    <View>
      <Text>Hello</Text>
    </View>
  );
};

export default Test;

const styles = StyleSheet.create({});
